export default function Modal({ open, title, children, onClose }) { if (!open) return null; return <div className="fixed inset-0 z-50 grid place-items-center bg-black/60"><div className="glass-card-strong p-5 max-w-lg w-[92vw]"><div className="flex items-center justify-between"><div className="font-black">{title}</div><button className="surface-button" onClick={onClose}>Sulje</button></div><div className="mt-4">{children}</div></div></div>; }
